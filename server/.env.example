# Environment Variables for Garage360 Server
# Copy this file to .env and update with your actual values

# ===========================================
# SUPABASE CONFIGURATION (Recommended)
# ===========================================
# Get these from: https://app.supabase.com/project/_/settings/api

# Supabase Project URL (required for Supabase)
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous Key (for client-side operations)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (for server-side operations - KEEP SECRET!)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# Supabase Database Connection String (Direct connection)
# Get this from: https://app.supabase.com/project/_/settings/database
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
SUPABASE_DB_URL=postgresql://postgres.your-project-ref:password@aws-0-region.pooler.supabase.com:5432/postgres

# ===========================================
# OR LOCAL POSTGRESQL (Alternative)
# ===========================================
# Use these if you want to use local PostgreSQL instead of Supabase
DB_HOST=localhost
DB_PORT=5432
DB_NAME=garage360
DB_USER=garage360_app
DB_PASSWORD=garage360_secure_pass_2025
DATABASE_URL=postgresql://garage360_app:garage360_secure_pass_2025@localhost:5432/garage360

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Security
BCRYPT_ROUNDS=10

# Logging
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:3000

# ===========================================
# IMPORTANT NOTES:
# ===========================================
# 1. If SUPABASE_DB_URL is set, it will be used for database connection
# 2. If SUPABASE_DB_URL is not set, DB_HOST/DB_PORT/etc will be used
# 3. SUPABASE_URL and SUPABASE_SERVICE_KEY are used for Supabase auth features
# 4. Never commit your actual .env file with secrets to git!
